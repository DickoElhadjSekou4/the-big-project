<h1>Tous les potins</h1>
<% @gossips.each do |gossip| %>
  <div>
    <h3><%= gossip.title %></h3>
    <p>Par : <%= gossip.author.first_name %></p>
    <%= link_to "Voir plus", "/gossips/#{gossip.id}" %>
  </div>
<% end %>
